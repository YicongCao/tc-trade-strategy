---
description: 本仓库的提交与推送约定
alwaysApply: true
---

# 变更后必须推送到远程

**每次完成变更后，提交并推送到 `origin main`，不需要再问。** 这是用户的明确要求。

```bash
git add -A && git commit -m "..." && git push origin main
```

## 提交约定

- 前缀用 `chore:` / `docs:` / `feat:` / `fix:`，摘要用中文
- 正文说明**为什么**这样改，而不是罗列改了什么文件
- 关注点不同时拆成多个提交（例如"依赖参考文档"与"策略内容"分开）
- 用 HEREDOC 传多行提交信息，避免格式错乱

## 推送前检查

推送到 GitHub 前扫一遍凭证类信息：

```bash
rg -i "api[_-]?key|secret|password|Bearer |access[_-]?token|BEGIN .*PRIVATE KEY"
```

**可以入库**：

- 平台字段名，如 `ai_provider_id`、`stop_loss_pct`
- `strategy_id` 等对象 ID 的真实 UUID——用户已确认没问题。这些 ID 没有 OAuth token 用不了，写进 `strategies/` 的 YAML 头能让仓库自洽，直接拿去调 `get_strategy_profile` 回读核对
- 持仓标的、参数、价位等策略内容

**不可以入库**：

- API Key、OAuth token、券商凭证、私钥
- `user_id`（账户级标识，与对象 ID 不同，无需写进仓库）

## 远程

`origin` → `git@github.com:YicongCao/tc-trade-strategy.git`，默认分支 `main`。
