---
description: 平台风控阈值告警的处置口径，哪些该改哪些是误报
alwaysApply: true
---

# 平台风控告警：先分类，不要照单全改

平台策略编辑页会周期性报「风控配置异常」。这批告警是**通用规则，不理解策略意图**，照单全改会让策略变差。已经论证过两轮，结论固化在这里，**不要重新论证**。完整推导见 `reviews/2026-08-11-risk-config-fix.md`。

## 该改：冷却期短于或等于分析间隔

这类是真 bug。冷却期的语义是"距上次交易不足 N 分钟不许再动"，若 N ≤ `analysis_interval`，两轮之间天然超过冷却窗口，**约束在任何情况下都不触发**。

修的时候不要只设成"比间隔大一点"，按策略自身的时间尺度定，当前已生效的取值：

| 策略 | 间隔 | 同向 / 反向冷却 |
| --- | --- | --- |
| 长持标普 500 | 240 | 1440 / 1440 |
| 标普500均值回归 | 120 | 480 / 480 |
| DRAM 网格 | 30 | 240 / 240 |
| XPEV 网格（已暂停） | 30 | 240 / 240 |
| 主题动量赌狗 | 240 | 480 / 480 |

## 不该改：止损 / 止盈 / 日内熔断「过宽」

告警原文大意是「止损线 50% 过宽，要亏掉三成本金才触发，实际接近于没有止损」。**描述属实，但它描述的是有意为之的结果。**

四条依据：

1. **这几个字段必填且没有关闭开关。** 服务端校验范围是 `stop_loss_pct` 0.5–50、`daily_pnl_stop_pct` −50 ~ −0.5，**50 与 −50 就是合法区间内最接近「禁用」的取值**。
2. **在 AI 策略路径上它们不是硬触发**，而是以「软参考」形式写进 AI 请求的文字提示。平台功能页称其为"独立硬止损"，与实际行为不符。真正的机制约束是 `trading_mode`。
3. **`trading_mode: open_only` 的策略根本不能卖**（如长持标普 500），止损/止盈/熔断全是卖出侧触发条件，改成任何数字都不会执行，是死字段。
4. **给网格策略收紧止损是主动制造伤害**。LLM 会收到"浮亏接近 X% 时考虑平仓"的提示，而网格的核心逻辑恰恰是**越跌越买**。网格的退出规则写在提示词的硬性规则里（如 DRAM 跌破 34.92 清仓、XPEV 跌破 10.67 降为底仓），比一个百分比阈值精确得多。

**这批告警会在每次扫描时重复出现，属于预期内噪声。** 遇到时直接引用本规则说明，不要改配置，也不要重新推导一遍。

## 改动时的硬性注意事项

**`risk_config` 是整体替换，不做深合并。** 曾经因此把主题动量赌狗的三条组合级防线（`total_drawdown_stop_pct` / `stop_loss_frequency_limit` / `portfolio_cooldown_minutes`）整个冲掉。

正确做法：

1. 先 `get_strategy_profile` 读出**完整**的 `risk_config`
2. 只改目标字段，其余原样回传
3. 请求用户确认时，把**未改动的字段也列出来**供核对
4. `confirm_proposal` 之后必须 `get_strategy_profile` 回读，确认没有字段丢失

## 供应商参数不在 MCP 能改的范围内

最大输出长度、`enable_thinking`、`temperature` 这些是 **AI 供应商**的属性，没有对应的 MCP 写工具，只能让用户在平台「AI 供应商」页改。MCP 能做的只有用 `update_strategy` 把策略换到另一个已存在的供应商。
