# tc-trade-strategy

交易策略研究仓库：用来编写、回测和对比各类量化策略（网格、动量、均值回归等）。

目标是让每个策略都是一个独立、可插拔的模块，共用同一套数据加载、回测引擎和评估指标，方便在相同数据、相同口径下横向对比表现。

## 目录结构

```
strategies/   # 各策略实现，一个文件一个策略
backtest/     # 回测引擎、撮合与手续费/滑点模型
data/         # 行情数据加载与缓存（原始数据不入库）
metrics/      # 收益、回撤、夏普等评估指标
notebooks/    # 探索性分析与结果可视化
tests/        # 单元测试
dependencies/ # 外部系统能力参考（Trade Copilot 平台与 MCP 工具）
```

写策略前先读 `dependencies/README.md`，里面说明了哪些能力可以直接调平台、哪些必须本地自建。

## 策略清单

| 策略 | 类型 | 形态 | 状态 |
| --- | --- | --- | --- |
| [DRAM 网格](strategies/grid-dram.md) | 震荡 / 区间 | 参数规格，配置到 Trade Copilot 运行 | 待启动，前提未验证 |
| [XPEV 网格](strategies/grid-xpev.md) | 震荡 / 区间 | 参数规格，配置到 Trade Copilot 运行 | 待启动，需先在纯网格与底仓混合间选型 |
| [价格行为日内 15m](strategies/price-action-intraday-15m.md) | 趋势 + 反转，双向 | 自由裁量手册，不可直接回测 | 手册已成型 |
| 动量 Momentum | 趋势跟随 | 计划中 | 计划中 |
| 均值回归 Mean Reversion | 统计套利 | 计划中 | 计划中 |
| 双均线突破 MA Breakout | 趋势跟随 | 计划中 | 计划中 |

"形态"一列区分两类东西：**参数规格**可以直接配置到平台或喂给回测引擎；**自由裁量手册**依赖人的判断，无法参数化，别误当成可回测的量化规则。

## 计划

**第一阶段：打地基**

- 确定标的与数据源（先用日线/小时线的公开数据跑通流程）
- 实现数据加载与本地缓存
- 实现最小可用回测引擎：按 K 线推进、下单撮合、持仓与资金管理
- 实现基础指标：累计收益、年化收益、最大回撤、夏普、胜率

**第二阶段：跑通第一个策略**

- 定义统一的策略接口（输入行情，输出目标仓位或信号）
- 先实现网格策略，作为验证引擎正确性的基准
- 补上手续费与滑点，确认结果不是靠零成本假设撑起来的

**第三阶段：扩展策略与参数研究**

- 加入动量、均值回归、突破策略
- 参数网格搜索 + 样本内/样本外划分，观察参数敏感性
- 统一的策略对比报告（同一份数据、同一套指标）

**第四阶段：走向实盘（可选）**

- 接入交易所 API，先跑纸面交易
- 风控：单笔上限、最大持仓、日内亏损熔断
- 日志、监控与告警

## 约定

- 策略只负责产出信号，不直接调用下单接口，方便回测与实盘复用同一份逻辑
- 任何回测结果都要注明数据区间、手续费与滑点假设，否则不作为结论
- 密钥、API Key 一律走环境变量，不进仓库

## 免责声明

本仓库内容仅用于技术研究与学习，不构成任何投资建议。实盘交易风险自负。
